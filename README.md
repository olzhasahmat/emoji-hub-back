# Emoji Hub — Backend 🎯

Backend часть проекта **Emoji Hub** — это REST API, реализованное на Spring Boot, которое выступает промежуточным слоем между frontend-приложением и внешними сервисами (EmojiHub API и LLM API).

Backend отвечает за:
- получение и обработку данных об эмодзи,
- фильтрацию, поиск и сортировку,
- инкапсуляцию вызовов внешних API,
- обеспечение безопасности и расширяемости приложения.

---

## 🧩 Краткое описание проекта

Emoji Hub Backend предоставляет API для работы с эмодзи:
- получение списка эмодзи,
- поиск по названию,
- фильтрация по категориям,
- сортировка,
- получение детальной информации об эмодзи,
- (опционально) генерация атмосферного описания эмодзи с использованием LLM.

Все внешние API вызываются **исключительно с серверной стороны**, в соответствии с требованиями технического задания.

---

## 🚀 Установка и запуск

### Требования
- Java 17+
- Maven 3.8+
- Git

### Установка

```bash
git clone https://github.com/your-username/emoji-hub-backend.git
cd emoji-hub-backend

mvn clean install
mvn spring-boot:run

http://localhost:8080

GET http://localhost:8080/api/emojis

Процесс проектирования и разработки
	1.	Анализ требований технического задания.
	2.	Принятие решения о разделении frontend и backend.
	3.	Проектирование REST API с учётом масштабируемости.
	4.	Реализация слоя сервисов для работы с внешним EmojiHub API.
	5.	Добавление логики поиска, фильтрации и сортировки на сервере.
	6.	Подготовка архитектуры для последующего подключения LLM.
	7.	Реализация CORS и прокси-логики для безопасной работы с frontend.

⸻

🧠 Уникальные подходы и методологии
	•	Backend-for-Frontend (BFF) — backend адаптирован под нужды frontend.
	•	Инкапсуляция внешних API — frontend не знает о сторонних сервисах.
	•	Генерация стабильных ID для эмодзи, так как внешний API не предоставляет уникальные идентификаторы.
	•	Чистая слоистая архитектура (Controller → Service → Client).
	•	Возможность кэширования данных без изменения API.

⸻

⚖️ Принятые компромиссы
	•	В текущей версии не используется база данных — данные берутся напрямую из внешнего API.
	•	Избранные эмодзи не сохраняются на backend (можно добавить позже).
	•	Используется in-memory cache вместо Redis для упрощения деплоя.
	•	Обработка ошибок внешнего API минимальна (для MVP).

⸻

🐞 Известные проблемы и ограничения
	•	Внешний EmojiHub API может быть недоступен или работать нестабильно.
	•	Не все эмодзи имеют заполненное поле htmlCode.
	•	LLM-описания могут быть обобщёнными и зависеть от качества prompt’а.
	•	Отсутствует rate limiting для внешних API.

⸻

🛠 Выбор технического стека

Почему Spring Boot:
	•	Надёжный и широко используемый фреймворк в production.
	•	Отличная поддержка REST API.
	•	Удобная работа с HTTP-клиентами (WebClient).
	•	Простота добавления безопасности, кэша и мониторинга.
	•	Хорошо масштабируется и деплоится.

Используемые технологии:
	•	Java 17
	•	Spring Boot 3
	•	Spring Web
	•	WebClient
	•	Maven
	•	Lombok

Данный стек позволяет быстро разработать MVP и безболезненно масштабировать проект в будущем.

⸻

🌱 Возможные улучшения
	•	Добавление базы данных (PostgreSQL).
	•	Реализация избранного и пользователей.
	•	Подключение Redis.
	•	Добавление Swagger / OpenAPI.
	•	Rate limiting и retry-механизмы.
	•	Dockerизация приложения.